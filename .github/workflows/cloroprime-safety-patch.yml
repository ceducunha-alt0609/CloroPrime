name: CloroPrime safety patch
on:
  push:
    paths:
      - 'cloroprime_patch_safety.py'
      - '.github/workflows/cloroprime-safety-patch.yml'
jobs:
  patch:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - run: python cloroprime_patch_safety.py
      - run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add index.html sw.js
          git diff --cached --quiet || git commit -m 'Harden CloroPrime state backup and drum scaling'
          git push
